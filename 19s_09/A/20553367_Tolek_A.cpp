#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    int a, b;
    while (t--) {
        cin >> a >> b;
        cout << a << " " << (a << 1) << endl;
    }
    return 0;
}